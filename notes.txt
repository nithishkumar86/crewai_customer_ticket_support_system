Agent completes Task
        ↓
🛡️ Guardrail fires immediately   ← raw JSON available, pydantic = None
        ↓
  ├── (False, "error") → Agent retries task
  └── (True, output)  → CrewAI parses pydantic
                                ↓
                        Move to next Task


exact life cycle

Task 1 Agent finishes
    ↓
Guardrail 1 validates output.raw  ✅
    ↓
CrewAI fills output.pydantic
    ↓
Task 2 starts
    ↓
Guardrail 2 validates output.raw  ✅
    ↓
CrewAI fills output.pydantic
    ↓
Task 3 starts (with context from Task 1 + Task 2)
    ↓
Guardrail 3 validates output.raw  ✅
    ↓
Task 4 starts (with context from Task 1+2+3)
    ↓
Callback fires → Email sent 📧


🔥 The Real CrewAI Guardrail Lifecycle

Agent completes task
        ↓
🛡️ Guardrail fires IMMEDIATELY
   (before pydantic parsing)
   (before moving to next task)
        ↓
   output.raw     ✅ available
   output.pydantic ❌ None
   output.json_dict❌ None
        ↓
  ├── (False) → Agent RETRIES
  └── (True)  → CrewAI parses pydantic → Next Task


💡 Key Teaching Point for Your Video

"Guardrail is the gatekeeper between tasks — it fires the moment an agent finishes, before CrewAI does anything with the output.
That's why we always use json.loads(output.raw) inside guardrails — pydantic hasn't been populated yet!"


Great question Boss! 🎯

When agent finishes — CrewAI wraps everything in TaskOutput object:
pythonTaskOutput(
    raw        = '{"category":"Account","confidence_score":0.95,...}'  # ✅ always available — pure JSON string
    pydantic   = None   # ❌ not yet — filled AFTER guardrail passes
    json_dict  = None   # ❌ not yet — filled AFTER guardrail passes
    description= "classify_ticket_task description..."
    agent      = "Expert Customer Support Ticket Classifier"
    name       = "classify_ticket_task"
)

So Inside Guardrail — Only raw is Reliable
python# ✅ Correct way inside guardrail
data   = json.loads(output.raw)
result = TicketClassification(**data)


# ❌ Never do this inside guardrail
output.pydantic.category    # → None → AttributeError
output.json_dict["category"] # → None → TypeError



After Guardrail Passes — Full Object Available
python# ✅ In next task context or callback
output.raw        # JSON string
output.pydantic   # TicketClassification model
output.json_dict  # {"category": "Account", .


Task(
    output_pydantic=TicketClassification  # ← this forces agent
)
```

CrewAI **instructs the agent internally** to always return output in **strict JSON format** matching the pydantic schema!

---

### So for ALL 4 tasks — same pattern:
```
Task 1 → output.raw = '{"category":"...", "confidence_score":..., "issue_summary":"..."}'
Task 2 → output.raw = '{"priority_level":"...", "escalate_to_human":..., "reason":"..."}'
Task 3 → output.raw = '{"suggested_solution":"...", "resolution_time":"...", "needs_followup":...}'
Task 4 → output.raw = '{"email_subject":"...", "email_body":"...", "sent_to":"..."}'
```

---

### Golden Rule Boss 👑
```
output_pydantic in Task
        ↓
Agent forced to return JSON
        ↓
output.raw = always valid JSON string
        ↓
Guardrail → json.loads(output.raw) ✅
Callback  → json.loads(output.raw) ✅